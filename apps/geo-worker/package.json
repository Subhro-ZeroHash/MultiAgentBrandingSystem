{
  "name": "@bmas/geo-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput & node --watch dist/main.js",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "echo \"(no tests yet)\"",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@bmas/ai": "workspace:*",
    "@bmas/db": "workspace:*",
    "@bmas/shared": "workspace:*",
    "bullmq": "^5.80.9",
    "ioredis": "^5.11.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@bmas/config": "workspace:*",
    "@types/node": "^22.10.5",
    "eslint": "^9.18.0",
    "rimraf": "^6.1.3",
    "typescript": "^5.9.0"
  }
}
